class InjestWhereClause {
  constructor () {
    // TODO
  }

  above (lowerBound) {
    // TODO (lowerBound) => InjestRecordset
  }

  aboveOrEqual (lowerBound) {
    // TODO (lowerBound) => InjestRecordset
  }

  anyOf (...args) {
    // TODO (Array|...args) => InjestRecordset
  }

  anyOfIgnoreCase (...args) {
    // TODO (Array|...args) => InjestRecordset
  }

  below (upperBound) {
    // TODO (upperBound) => InjestRecordset
  }

  belowOrEqual (upperBound) {
    // TODO (upperBound) => InjestRecordset
  }

  between (lowerBound, upperBound, opts) {
    // TODO (lowerBound, upperBound, opts) => InjestRecordset
  }

  equals (value) {
    // TODO (value) => InjestRecordset
  }

  equalsIgnoreCase (value) {
    // TODO (value) => InjestRecordset
  }

  inAnyRange (ranges, opts) {
    // TODO (ranges, opts) => InjestRecordset
  }

  noneOf (...args) {
    // TODO (Array|...args) => InjestRecordset
  }

  notEqual (value) {
    // TODO (value) => InjestRecordset
  }

  startsWith (value) {
    // TODO (value) => InjestRecordset
  }

  startsWithAnyOf (...args) {
    // TODO (Array|...args) => InjestRecordset
  }

  startsWithAnyOfIgnoreCase (...args) {
    // TODO (Array|...args) => InjestRecordset
  }

  startsWithIgnoreCase (value) {
    // TODO (value) => InjestRecordset
  }
}
